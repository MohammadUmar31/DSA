int maxNestingDepth(string s) {
        int count=0;
        int maxi=0;
        for(auto ch : s){
            if(ch=='('){
                count++;
                maxi=max(maxi,count);
            }
            else if(ch==')') count--;
        }
        return maxi;
}
