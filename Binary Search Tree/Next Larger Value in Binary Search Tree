int nextLargerValue(Node* root, int X) {
        int ans=-1;
        while(root){
            if(X<root->data){
                ans=root->data;
                root=root->left;
            }
            else{
                root=root->right;
            }
        }
        return ans;
    }
