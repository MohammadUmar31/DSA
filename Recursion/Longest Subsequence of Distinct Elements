int findLongestDistinctSubsequence(vector<int>& arr, int n) {
        // Write your code here...
        unordered_set<int> s;
        for(int i=0;i<n;i++){
            s.insert(arr[i]);
        }
        return s.size();
    }
