int maximizeShareProfit(vector<int> arr){
        int n=arr.size();
        int buy=arr[0];
        int profit=0;
        for(int i=1;i<n;i++){
            buy=min(buy,arr[i]);
            profit=max(profit,arr[i]-buy);
        }
        return profit;
}
